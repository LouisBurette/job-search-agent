# Job Agent Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construire un workflow n8n qui collecte des offres d'emploi depuis 9 sources, filtre par règles, score via Claude API, et envoie un digest email HTML hebdomadaire classé par pertinence.

**Architecture:** Workflow n8n linéaire déclenché par cron chaque lundi matin. Les 9 sources sont collectées en parallèle, mergées, dédupliquées via Static Data, filtrées par règles JavaScript, scorées par Claude API, puis formatées en HTML et envoyées par SMTP.

**Tech Stack:** n8n self-hosted, Claude API (claude-sonnet-4-6), SMTP, RSS feeds, HTTP Request nodes, JavaScript (Function nodes), Static Data (persistance n8n)

---

## Structure des fichiers

```
job-agent/
├── workflows/
│   └── job-agent.json          # Export du workflow n8n (mis à jour à chaque tâche)
├── scripts/
│   ├── filter.js               # Logique de filtrage par règles (développée hors n8n, copiée dans Function node)
│   ├── normalize.js            # Normalisation des offres (idem)
│   ├── score-prompt.js         # Construction du prompt LLM
│   └── email-template.js       # Génération HTML du digest
└── docs/
    └── superpowers/
        ├── specs/2026-04-17-job-agent-design.md
        └── plans/2026-04-17-job-agent.md
```

> **Note n8n :** Chaque tâche construit une section du workflow dans l'UI n8n. Après chaque tâche, exporter le workflow (`⋮ → Download`) et le sauvegarder dans `workflows/job-agent.json`, puis committer.

---

## Task 1 : Setup projet + nœud Config

**Files:**
- Create: `workflows/job-agent.json`
- Create: `scripts/normalize.js`

### Objectif
Créer le squelette du workflow avec le nœud Config central et vérifier que n8n est opérationnel.

- [ ] **Step 1: Vérifier que n8n est accessible**

Ouvrir l'interface n8n self-hosted. Vérifier qu'on peut créer un nouveau workflow.

- [ ] **Step 2: Créer un nouveau workflow nommé "Job Agent"**

Dans n8n UI : `+ New Workflow` → nommer `Job Agent`.

- [ ] **Step 3: Ajouter un nœud "Set" pour la configuration**

Ajouter un nœud **Set** nommé `Config`. Passer en mode JSON et coller :

```json
{
  "postes": ["Product Manager", "Data Product Manager"],
  "contrats": ["CDI"],
  "salaire_min": 45000,
  "teletravail": "full",
  "secteurs_ok": ["tech", "impact", "SaaS"],
  "secteurs_nok": ["défense", "gambling", "crypto"],
  "experience_min": 3,
  "experience_max": 5,
  "stack": ["n8n", "claude code", "Notion", "Jira", "SQL"],
  "keywords": ["n8n", "claude code", "ai agents", "LLM", "automation"],
  "langues": ["fr", "en", "es"],
  "max_age_days": 7,
  "top_n": 15,
  "geo": ["Donostia", "San Sebastián", "remote", "full remote", "télétravail"]
}
```

- [ ] **Step 4: Créer le fichier normalize.js**

Ce code sera copié dans les Function nodes de normalisation. Créer `scripts/normalize.js` :

```js
// Normalise une offre brute vers le format unifié
// Input: rawItem (objet avec des champs variables selon la source)
// Output: { titre, entreprise, url, description, date, source, localisation, salaire }
function normalize(rawItem, source) {
  return {
    titre: rawItem.titre || rawItem.title || rawItem.positionName || '',
    entreprise: rawItem.entreprise || rawItem.company || rawItem.companyName || '',
    url: rawItem.url || rawItem.link || rawItem.jobUrl || '',
    description: rawItem.description || rawItem.content || rawItem.snippet || '',
    date: rawItem.date || rawItem.pubDate || rawItem.publishedAt || new Date().toISOString(),
    source: source,
    localisation: rawItem.localisation || rawItem.location || rawItem.region || '',
    salaire: rawItem.salaire || rawItem.salary || rawItem.compensation || '',
  };
}
```

- [ ] **Step 5: Exporter le workflow et committer**

Dans n8n : `⋮ → Download` → sauvegarder sous `workflows/job-agent.json`.

```bash
cd /Users/louisburette/Desktop/job-agent
git add workflows/job-agent.json scripts/normalize.js
git commit -m "feat: workflow skeleton + config node + normalize helper"
```

---

## Task 2 : Collecteurs RSS internationaux

**Files:**
- Modify: `workflows/job-agent.json`

### Objectif
Collecter les offres depuis Remotive, We Work Remotely et Jobs That Make Sense via RSS/API.

- [ ] **Step 1: Ajouter un nœud Cron (trigger)**

Ajouter un nœud **Schedule Trigger** :
- Intervalle : `Weekly`
- Jour : `Monday`
- Heure : `08:00`

Le connecter au nœud `Config`.

- [ ] **Step 2: Ajouter le collecteur Remotive**

Après Config, ajouter un nœud **RSS Feed Read** nommé `Remotive RSS` :
- URL : `https://remotive.com/remote-jobs/rss?category=product&limit=50`

Ajouter un nœud **Function** nommé `Normalize Remotive` connecté après :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => new Date(item.json.pubDate) >= cutoff)
  .map(item => ({
    json: {
      titre: item.json.title || '',
      entreprise: item.json.creator || '',
      url: item.json.link || '',
      description: item.json.content || item.json.contentSnippet || '',
      date: item.json.pubDate || '',
      source: 'Remotive',
      localisation: 'Remote',
      salaire: '',
    }
  }));
```

- [ ] **Step 3: Tester le collecteur Remotive**

Dans n8n : clic droit sur `Normalize Remotive` → `Execute Node`. Vérifier que des offres apparaissent avec les champs normalisés.

- [ ] **Step 4: Ajouter le collecteur We Work Remotely**

Ajouter un nœud **RSS Feed Read** nommé `WWDR RSS` :
- URL : `https://weworkremotely.com/categories/remote-product-jobs.rss`

Ajouter un nœud **Function** nommé `Normalize WWDR` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => new Date(item.json.pubDate) >= cutoff)
  .map(item => {
    const titleParts = (item.json.title || '').split(' at ');
    return {
      json: {
        titre: titleParts[0]?.trim() || item.json.title || '',
        entreprise: titleParts[1]?.trim() || '',
        url: item.json.link || '',
        description: item.json.content || item.json.contentSnippet || '',
        date: item.json.pubDate || '',
        source: 'We Work Remotely',
        localisation: 'Remote',
        salaire: '',
      }
    };
  });
```

- [ ] **Step 5: Ajouter le collecteur Jobs That Make Sense**

Ajouter un nœud **HTTP Request** nommé `JTMS API` :
- Method : `GET`
- URL : `https://api.jobsthatmakesense.com/jobs?format=json`
- Si l'API n'est pas disponible, utiliser le RSS : `https://jobsthatmakesense.com/feed`

Ajouter un nœud **Function** nommé `Normalize JTMS` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

// Adapter selon la structure réelle de la réponse API
const jobs = $input.first().json.jobs || $input.all().map(i => i.json);

return jobs
  .filter(job => new Date(job.published_at || job.pubDate) >= cutoff)
  .map(job => ({
    json: {
      titre: job.title || job.titre || '',
      entreprise: job.company || job.entreprise || '',
      url: job.url || job.link || '',
      description: job.description || job.content || '',
      date: job.published_at || job.pubDate || '',
      source: 'Jobs That Make Sense',
      localisation: job.location || 'Remote',
      salaire: job.salary || '',
    }
  }));
```

- [ ] **Step 6: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter le workflow depuis n8n UI → workflows/job-agent.json
git add workflows/job-agent.json
git commit -m "feat: add RSS collectors for Remotive, WWDR, JTMS"
```

---

## Task 3 : Collecteurs sources espagnoles

**Files:**
- Modify: `workflows/job-agent.json`

### Objectif
Collecter les offres depuis Indeed.es, Welcome to the Jungle, InfoJobs, Tecnoempleo, Computrabajo, LinkedIn.

- [ ] **Step 1: Ajouter Indeed.es RSS**

Ajouter un nœud **RSS Feed Read** nommé `Indeed.es RSS` :
- URL : `https://es.indeed.com/rss?q=product+manager&l=Donostia&sort=date&fromage=7`

Ajouter un nœud **Function** nommé `Normalize Indeed` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => new Date(item.json.pubDate) >= cutoff)
  .map(item => ({
    json: {
      titre: item.json.title || '',
      entreprise: item.json.source || '',
      url: item.json.link || '',
      description: item.json.contentSnippet || item.json.content || '',
      date: item.json.pubDate || '',
      source: 'Indeed.es',
      localisation: item.json['location'] || 'Donostia / Remote',
      salaire: '',
    }
  }));
```

Répéter avec une seconde URL pour le full remote :
- URL : `https://es.indeed.com/rss?q=product+manager&l=remote&remotejob=032b3046-06a3-4876-8dfd-474eb5e7ed11&sort=date&fromage=7`

- [ ] **Step 2: Ajouter Welcome to the Jungle RSS**

Ajouter un nœud **RSS Feed Read** nommé `WTTJ RSS` :
- URL : `https://www.welcometothejungle.com/fr/jobs/rss?refinementList%5Boffices.country_code%5D%5B%5D=ES&refinementList%5Bcontract_type%5D%5B%5D=CDI`

Ajouter un nœud **Function** nommé `Normalize WTTJ` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => new Date(item.json.pubDate) >= cutoff)
  .map(item => ({
    json: {
      titre: item.json.title || '',
      entreprise: item.json.creator || '',
      url: item.json.link || '',
      description: item.json.contentSnippet || item.json.content || '',
      date: item.json.pubDate || '',
      source: 'Welcome to the Jungle',
      localisation: item.json.category || 'Espagne',
      salaire: '',
    }
  }));
```

- [ ] **Step 3: Ajouter InfoJobs API**

InfoJobs nécessite une clé API. Créer un compte développeur sur [developer.infojobs.net](https://developer.infojobs.net).

Ajouter un nœud **HTTP Request** nommé `InfoJobs API` :
- Method : `GET`
- URL : `https://api.infojobs.net/api/9/offer`
- Query params :
  - `q` : `product manager`
  - `city` : `donostia-san-sebastian`
  - `teleworking` : `2` (full remote) ou `1` (partial)
  - `contractType` : `1` (CDI/Indefinido)
  - `maxResults` : `50`
- Auth : `Basic Auth` (clé API InfoJobs en base64)

Ajouter un nœud **Function** nommé `Normalize InfoJobs` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

const offers = $input.first().json.items || [];

return offers
  .filter(offer => new Date(offer.updated) >= cutoff)
  .map(offer => ({
    json: {
      titre: offer.title || '',
      entreprise: offer.author?.name || '',
      url: offer.link || '',
      description: offer.requirementMin || offer.description || '',
      date: offer.updated || '',
      source: 'InfoJobs',
      localisation: offer.city?.value || offer.province?.value || '',
      salaire: offer.salaryDescription || '',
    }
  }));
```

- [ ] **Step 4: Ajouter Tecnoempleo RSS**

Ajouter un nœud **RSS Feed Read** nommé `Tecnoempleo RSS` :
- URL : `https://www.tecnoempleo.com/rss/?te=1&pais=España&telework=1`

Ajouter un nœud **Function** nommé `Normalize Tecnoempleo` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => new Date(item.json.pubDate) >= cutoff)
  .map(item => ({
    json: {
      titre: item.json.title || '',
      entreprise: item.json.creator || '',
      url: item.json.link || '',
      description: item.json.contentSnippet || '',
      date: item.json.pubDate || '',
      source: 'Tecnoempleo',
      localisation: item.json.category || 'Espagne',
      salaire: '',
    }
  }));
```

- [ ] **Step 5: Ajouter Computrabajo RSS**

Ajouter un nœud **RSS Feed Read** nommé `Computrabajo RSS` :
- URL : `https://www.computrabajo.es/rss/ofertas-de-trabajo-de-product-manager.xml`

Ajouter un nœud **Function** nommé `Normalize Computrabajo` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => new Date(item.json.pubDate) >= cutoff)
  .map(item => ({
    json: {
      titre: item.json.title || '',
      entreprise: item.json.creator || item.json.source || '',
      url: item.json.link || '',
      description: item.json.contentSnippet || '',
      date: item.json.pubDate || '',
      source: 'Computrabajo',
      localisation: item.json.category || 'Espagne',
      salaire: '',
    }
  }));
```

- [ ] **Step 6: Ajouter LinkedIn RSS**

LinkedIn RSS de recherche (limité sans auth) :
- URL : `https://www.linkedin.com/jobs/search/?keywords=product+manager&location=Donostia&f_WT=2&f_TPR=r604800&f_JT=F`

> ⚠️ LinkedIn bloque souvent le scraping. Si le RSS est inaccessible, sauter cette source et noter dans le README.

Ajouter un nœud **RSS Feed Read** nommé `LinkedIn RSS` avec l'URL ci-dessus.

Ajouter un nœud **Function** nommé `Normalize LinkedIn` :

```js
const config = $('Config').first().json;
const maxAge = config.max_age_days;
const cutoff = new Date(Date.now() - maxAge * 24 * 60 * 60 * 1000);

return $input.all()
  .filter(item => {
    try { return new Date(item.json.pubDate) >= cutoff; }
    catch { return true; }
  })
  .map(item => ({
    json: {
      titre: item.json.title || '',
      entreprise: item.json.creator || '',
      url: item.json.link || '',
      description: item.json.contentSnippet || '',
      date: item.json.pubDate || '',
      source: 'LinkedIn',
      localisation: 'Donostia / Remote',
      salaire: '',
    }
  }));
```

- [ ] **Step 7: Tester chaque collecteur indépendamment**

Pour chaque nœud `Normalize *` : clic droit → `Execute Node`. Vérifier que les champs `titre`, `url`, `source` sont remplis.

Si un RSS retourne 0 résultats, vérifier l'URL manuellement dans le navigateur.

- [ ] **Step 8: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter le workflow → workflows/job-agent.json
git add workflows/job-agent.json
git commit -m "feat: add Spanish job board collectors (Indeed.es, WTTJ, InfoJobs, Tecnoempleo, Computrabajo, LinkedIn)"
```

---

## Task 4 : Merge + déduplication

**Files:**
- Modify: `workflows/job-agent.json`

### Objectif
Fusionner toutes les offres en une seule liste, puis exclure celles déjà envoyées lors des semaines précédentes.

- [ ] **Step 1: Ajouter un nœud Merge**

Connecter tous les nœuds `Normalize *` à un nœud **Merge** nommé `Merge All Sources` :
- Mode : `Append` (concatène toutes les entrées)

- [ ] **Step 2: Ajouter la déduplication par URL (doublons dans la semaine)**

Ajouter un nœud **Function** nommé `Dedup Same Week` connecté après Merge :

```js
const seen = new Set();
return $input.all().filter(item => {
  const url = item.json.url;
  if (!url || seen.has(url)) return false;
  seen.add(url);
  return true;
});
```

- [ ] **Step 3: Ajouter la déduplication historique (Static Data)**

Ajouter un nœud **Function** nommé `Dedup Historical` :

```js
// Récupérer les URLs déjà envoyées (persistées entre exécutions)
const staticData = $getWorkflowStaticData('global');
if (!staticData.sentUrls) staticData.sentUrls = [];

// Nettoyer les URLs de plus de 30 jours
const thirtyDaysAgo = Date.now() - 30 * 24 * 60 * 60 * 1000;
staticData.sentUrls = staticData.sentUrls.filter(entry => entry.ts > thirtyDaysAgo);

const sentSet = new Set(staticData.sentUrls.map(e => e.url));

// Filtrer les offres déjà envoyées
const newOffers = $input.all().filter(item => !sentSet.has(item.json.url));

return newOffers;
```

- [ ] **Step 4: Tester le merge**

Exécuter jusqu'au nœud `Dedup Same Week`. Vérifier que :
- Toutes les sources sont présentes dans le résultat
- Les doublons d'URL sont supprimés

- [ ] **Step 5: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter → workflows/job-agent.json
git add workflows/job-agent.json
git commit -m "feat: merge all sources + deduplication (weekly + historical)"
```

---

## Task 5 : Filtrage par règles

**Files:**
- Create: `scripts/filter.js`
- Modify: `workflows/job-agent.json`

### Objectif
Éliminer les offres qui ne matchent pas les critères durs avant d'appeler le LLM.

- [ ] **Step 1: Créer le fichier filter.js**

```js
// scripts/filter.js
// Logique de filtrage copiée dans le Function node n8n

function matchesGeo(localisation, geo) {
  if (!localisation) return false;
  const loc = localisation.toLowerCase();
  return geo.some(g => loc.includes(g.toLowerCase()));
}

function matchesPoste(titre, description, postes) {
  const text = `${titre} ${description}`.toLowerCase();
  return postes.some(p => text.includes(p.toLowerCase()));
}

function matchesLangue(description, langues) {
  // Heuristique simple : mots français, anglais, espagnols
  const frWords = ['expérience', 'poste', 'entreprise', 'recherchons'];
  const enWords = ['experience', 'position', 'company', 'looking'];
  const esWords = ['experiencia', 'puesto', 'empresa', 'buscamos'];

  const desc = description.toLowerCase();
  const hasFr = frWords.some(w => desc.includes(w));
  const hasEn = enWords.some(w => desc.includes(w));
  const hasEs = esWords.some(w => desc.includes(w));

  if (langues.includes('fr') && hasFr) return true;
  if (langues.includes('en') && hasEn) return true;
  if (langues.includes('es') && hasEs) return true;
  // Si aucune détection, on garde l'offre (bénéfice du doute)
  if (!hasFr && !hasEn && !hasEs) return true;
  return false;
}

function matchesTeletravail(description, localisation) {
  const text = `${description} ${localisation}`.toLowerCase();
  return (
    text.includes('remote') ||
    text.includes('télétravail') ||
    text.includes('teletrabajo') ||
    text.includes('full remote') ||
    text.includes('100% remote') ||
    localisation.toLowerCase().includes('remote')
  );
}

function avoidsSecteur(description, titre, secteurs_nok) {
  const text = `${titre} ${description}`.toLowerCase();
  return !secteurs_nok.some(s => text.includes(s.toLowerCase()));
}

module.exports = { matchesGeo, matchesPoste, matchesLangue, matchesTeletravail, avoidsSecteur };
```

- [ ] **Step 2: Ajouter le nœud de filtrage dans n8n**

Ajouter un nœud **Function** nommé `Rule Filter` après `Dedup Historical` :

```js
const config = $('Config').first().json;

function matchesGeo(localisation, geo) {
  if (!localisation) return true; // Remote implicite si pas de localisation
  const loc = localisation.toLowerCase();
  return geo.some(g => loc.includes(g.toLowerCase()));
}

function matchesPoste(titre, description, postes) {
  const text = `${titre} ${description}`.toLowerCase();
  return postes.some(p => text.includes(p.toLowerCase()));
}

function matchesTeletravail(description, localisation) {
  const text = `${description} ${localisation}`.toLowerCase();
  return (
    text.includes('remote') ||
    text.includes('télétravail') ||
    text.includes('teletrabajo') ||
    text.includes('full remote') ||
    text.includes('100%')
  );
}

function avoidsSecteur(description, titre, secteurs_nok) {
  const text = `${titre} ${description}`.toLowerCase();
  return !secteurs_nok.some(s => text.includes(s.toLowerCase()));
}

function isRecent(dateStr, max_age_days) {
  try {
    const date = new Date(dateStr);
    const cutoff = new Date(Date.now() - max_age_days * 24 * 60 * 60 * 1000);
    return date >= cutoff;
  } catch { return true; }
}

return $input.all().filter(item => {
  const { titre, description, localisation, date, source } = item.json;

  // Critère géo : Donosti OU Remote
  const geoOk = matchesGeo(localisation, config.geo) || matchesTeletravail(description, localisation);
  if (!geoOk) return false;

  // Critère poste
  if (!matchesPoste(titre, description, config.postes)) return false;

  // Secteurs à éviter
  if (!avoidsSecteur(description, titre, config.secteurs_nok)) return false;

  // Date
  if (!isRecent(date, config.max_age_days)) return false;

  return true;
});
```

- [ ] **Step 3: Tester le filtrage**

Exécuter jusqu'au nœud `Rule Filter`. Vérifier que :
- Les offres hors géo sont exclues
- Les offres hors poste (ex: "Développeur Python") sont exclues
- Les offres des secteurs `nok` sont exclues
- Les offres de plus de 7 jours sont exclues

- [ ] **Step 4: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter → workflows/job-agent.json
git add workflows/job-agent.json scripts/filter.js
git commit -m "feat: rule-based filtering (geo, poste, secteurs, date)"
```

---

## Task 6 : Scoring LLM via Claude API

**Files:**
- Create: `scripts/score-prompt.js`
- Modify: `workflows/job-agent.json`

### Objectif
Appeler Claude API pour scorer chaque offre survivante (1-10) avec une justification courte.

- [ ] **Step 1: Créer le prompt builder**

Créer `scripts/score-prompt.js` :

```js
// scripts/score-prompt.js
// Construction du prompt LLM pour le scoring d'offre

function buildPrompt(config, offer) {
  return `Tu es un assistant de recherche d'emploi. Évalue la pertinence de cette offre pour ce profil.

PROFIL :
- Postes recherchés : ${config.postes.join(', ')}
- Contrats : ${config.contrats.join(', ')}
- Salaire min : ${config.salaire_min}€
- Télétravail : ${config.teletravail}
- Secteurs appréciés : ${config.secteurs_ok.join(', ')}
- Secteurs à éviter : ${config.secteurs_nok.join(', ')}
- Expérience : ${config.experience_min}-${config.experience_max} ans
- Stack / outils : ${config.stack.join(', ')}
- Keywords d'intérêt : ${config.keywords.join(', ')}
- Langues : ${config.langues.join(', ')}

OFFRE :
Titre : ${offer.titre}
Entreprise : ${offer.entreprise}
Source : ${offer.source}
Localisation : ${offer.localisation}
Salaire : ${offer.salaire || 'non précisé'}
Description : ${offer.description.substring(0, 1500)}

Réponds UNIQUEMENT en JSON valide, sans markdown, sans commentaire :
{ "score": <entier 1-10>, "raison": "<2 phrases max en français>" }`;
}

module.exports = { buildPrompt };
```

- [ ] **Step 2: Configurer la clé API Claude dans n8n**

Dans n8n : `Settings → Credentials → Add Credential → Anthropic API` → coller la clé API Claude.

- [ ] **Step 3: Ajouter le nœud de scoring LLM**

Ajouter un nœud **HTTP Request** nommé `Score LLM` après `Rule Filter` :
- Method : `POST`
- URL : `https://api.anthropic.com/v1/messages`
- Authentication : `Generic Credential Type` → utiliser la credential Anthropic
- Headers :
  - `x-api-key` : `{{ $credentials.anthropicApi.apiKey }}`
  - `anthropic-version` : `2023-06-01`
  - `content-type` : `application/json`
- Body (JSON) :

```json
{
  "model": "claude-sonnet-4-6",
  "max_tokens": 150,
  "messages": [
    {
      "role": "user",
      "content": "={{ $json.prompt }}"
    }
  ]
}
```

> Ce nœud sera précédé d'un nœud Function qui construit le prompt.

- [ ] **Step 4: Ajouter le nœud Function "Build Prompt"**

Insérer un nœud **Function** nommé `Build Prompt` AVANT `Score LLM` :

```js
const config = $('Config').first().json;
const offer = $input.first().json;

const prompt = `Tu es un assistant de recherche d'emploi. Évalue la pertinence de cette offre pour ce profil.

PROFIL :
- Postes recherchés : ${config.postes.join(', ')}
- Contrats : ${config.contrats.join(', ')}
- Salaire min : ${config.salaire_min}€
- Télétravail : ${config.teletravail}
- Secteurs appréciés : ${config.secteurs_ok.join(', ')}
- Secteurs à éviter : ${config.secteurs_nok.join(', ')}
- Expérience : ${config.experience_min}-${config.experience_max} ans
- Stack / outils : ${config.stack.join(', ')}
- Keywords d'intérêt : ${config.keywords.join(', ')}
- Langues : ${config.langues.join(', ')}

OFFRE :
Titre : ${offer.titre}
Entreprise : ${offer.entreprise}
Source : ${offer.source}
Localisation : ${offer.localisation}
Salaire : ${offer.salaire || 'non précisé'}
Description : ${(offer.description || '').substring(0, 1500)}

Réponds UNIQUEMENT en JSON valide, sans markdown, sans commentaire :
{ "score": <entier 1-10>, "raison": "<2 phrases max en français>" }`;

return [{ json: { ...offer, prompt } }];
```

- [ ] **Step 5: Ajouter le nœud Function "Parse LLM Response"**

Ajouter un nœud **Function** nommé `Parse Score` après `Score LLM` :

```js
const offer = $input.first().json;
const rawResponse = $input.first().json.content?.[0]?.text || '{}';

let score = null;
let raison = '';

try {
  const parsed = JSON.parse(rawResponse);
  score = parsed.score || null;
  raison = parsed.raison || '';
} catch (e) {
  score = null;
  raison = 'Score non disponible';
}

return [{ json: { ...offer, score, raison } }];
```

- [ ] **Step 6: Tester le scoring sur une offre**

Clic droit sur `Parse Score` → `Execute Node`. Vérifier que le JSON retourné contient `score` (entier 1-10) et `raison` (2 phrases).

- [ ] **Step 7: Ajouter le tri et le top_n**

Ajouter un nœud **Function** nommé `Sort & Top N` après que toutes les offres ont été scorées :

```js
const config = $('Config').first().json;
const offers = $input.all().map(i => i.json);

// Trier par score décroissant (null en dernier)
offers.sort((a, b) => {
  if (a.score === null) return 1;
  if (b.score === null) return -1;
  return b.score - a.score;
});

return offers.slice(0, config.top_n).map(o => ({ json: o }));
```

- [ ] **Step 8: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter → workflows/job-agent.json
git add workflows/job-agent.json scripts/score-prompt.js
git commit -m "feat: LLM scoring via Claude API + sort by score"
```

---

## Task 7 : Génération email HTML + envoi SMTP

**Files:**
- Create: `scripts/email-template.js`
- Modify: `workflows/job-agent.json`

### Objectif
Générer un digest HTML avec une carte par offre, et l'envoyer par email.

- [ ] **Step 1: Créer le template HTML**

Créer `scripts/email-template.js` :

```js
// scripts/email-template.js
// Généré dans le Function node n8n

function scoreColor(score) {
  if (score >= 8) return '#22c55e';  // vert
  if (score >= 5) return '#f59e0b';  // orange
  return '#ef4444';                   // rouge
}

function buildCard(offer) {
  const score = offer.score !== null ? offer.score : '?';
  const color = offer.score !== null ? scoreColor(offer.score) : '#94a3b8';

  return `
  <div style="border:1px solid #e2e8f0;border-radius:8px;padding:16px;margin-bottom:16px;font-family:sans-serif;">
    <div style="display:flex;justify-content:space-between;align-items:flex-start;">
      <div>
        <h3 style="margin:0 0 4px;font-size:16px;color:#1e293b;">${offer.titre}</h3>
        <p style="margin:0;color:#64748b;font-size:14px;">${offer.entreprise} · ${offer.source} · ${offer.localisation}</p>
        ${offer.salaire ? `<p style="margin:4px 0 0;color:#64748b;font-size:13px;">💰 ${offer.salaire}</p>` : ''}
      </div>
      <div style="background:${color};color:white;border-radius:50%;width:36px;height:36px;display:flex;align-items:center;justify-content:center;font-weight:bold;font-size:14px;flex-shrink:0;text-align:center;line-height:36px;">
        ${score}
      </div>
    </div>
    <p style="margin:10px 0 8px;font-size:13px;color:#475569;font-style:italic;">${offer.raison}</p>
    <a href="${offer.url}" style="display:inline-block;background:#3b82f6;color:white;padding:6px 14px;border-radius:4px;font-size:13px;text-decoration:none;">Voir l'offre →</a>
  </div>`;
}

function buildEmail(offers, failedSources) {
  const cards = offers.map(buildCard).join('');
  const warning = failedSources.length > 0
    ? `<p style="color:#ef4444;font-size:13px;margin-top:24px;">⚠️ Sources indisponibles cette semaine : ${failedSources.join(', ')}</p>`
    : '';

  return `
  <html>
  <body style="background:#f8fafc;padding:24px;">
    <div style="max-width:600px;margin:0 auto;">
      <h1 style="font-size:20px;color:#1e293b;margin-bottom:4px;">🎯 Job Digest — ${new Date().toLocaleDateString('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' })}</h1>
      <p style="color:#64748b;font-size:14px;margin-bottom:24px;">${offers.length} offre${offers.length > 1 ? 's' : ''} sélectionnée${offers.length > 1 ? 's' : ''}, classée${offers.length > 1 ? 's' : ''} par pertinence</p>
      ${cards}
      ${warning}
    </div>
  </body>
  </html>`;
}

module.exports = { buildEmail };
```

- [ ] **Step 2: Ajouter le nœud Function "Build Email HTML" dans n8n**

Ajouter un nœud **Function** nommé `Build Email HTML` après `Sort & Top N` :

```js
const offers = $input.all().map(i => i.json);

// Sources en erreur (transmises depuis le Error Handler — voir Task 8)
const failedSources = $('Track Errors').first().json.failedSources || [];

function scoreColor(score) {
  if (score >= 8) return '#22c55e';
  if (score >= 5) return '#f59e0b';
  return '#ef4444';
}

function buildCard(offer) {
  const score = offer.score !== null ? offer.score : '?';
  const color = offer.score !== null ? scoreColor(offer.score) : '#94a3b8';
  return `
  <div style="border:1px solid #e2e8f0;border-radius:8px;padding:16px;margin-bottom:16px;font-family:sans-serif;">
    <div style="display:flex;justify-content:space-between;align-items:flex-start;">
      <div>
        <h3 style="margin:0 0 4px;font-size:16px;color:#1e293b;">${offer.titre}</h3>
        <p style="margin:0;color:#64748b;font-size:14px;">${offer.entreprise} · ${offer.source} · ${offer.localisation}</p>
        ${offer.salaire ? `<p style="margin:4px 0 0;color:#64748b;font-size:13px;">💰 ${offer.salaire}</p>` : ''}
      </div>
      <div style="background:${color};color:white;border-radius:50%;width:36px;height:36px;display:flex;align-items:center;justify-content:center;font-weight:bold;font-size:14px;flex-shrink:0;text-align:center;line-height:36px;">${score}</div>
    </div>
    <p style="margin:10px 0 8px;font-size:13px;color:#475569;font-style:italic;">${offer.raison}</p>
    <a href="${offer.url}" style="display:inline-block;background:#3b82f6;color:white;padding:6px 14px;border-radius:4px;font-size:13px;text-decoration:none;">Voir l'offre →</a>
  </div>`;
}

const cards = offers.map(buildCard).join('');
const warning = failedSources.length > 0
  ? `<p style="color:#ef4444;font-size:13px;margin-top:24px;">⚠️ Sources indisponibles cette semaine : ${failedSources.join(', ')}</p>`
  : '';

const html = `<html><body style="background:#f8fafc;padding:24px;">
  <div style="max-width:600px;margin:0 auto;">
    <h1 style="font-size:20px;color:#1e293b;margin-bottom:4px;">🎯 Job Digest — ${new Date().toLocaleDateString('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' })}</h1>
    <p style="color:#64748b;font-size:14px;margin-bottom:24px;">${offers.length} offre${offers.length > 1 ? 's' : ''} sélectionnée${offers.length > 1 ? 's' : ''}</p>
    ${cards}${warning}
  </div></body></html>`;

return [{ json: { html, offerCount: offers.length, offers } }];
```

- [ ] **Step 3: Ajouter un nœud IF "Has Offers?"**

Ajouter un nœud **IF** nommé `Has Offers?` :
- Condition : `{{ $json.offerCount }} > 0`
- Branche `true` : continuer vers l'email
- Branche `false` : terminer le workflow (pas d'email)

- [ ] **Step 4: Configurer SMTP dans n8n**

Dans n8n : `Settings → Credentials → Add Credential → SMTP`.
Remplir avec les paramètres de ton serveur mail (ex: Gmail SMTP, Mailgun, Resend...).

- [ ] **Step 5: Ajouter le nœud Email**

Ajouter un nœud **Send Email** nommé `Send Digest` :
- From : `ton-email@domaine.com`
- To : `louisburette@[domaine].com`
- Subject : `🎯 Job Digest — {{ new Date().toLocaleDateString('fr-FR') }} ({{ $json.offerCount }} offres)`
- Email Type : `HTML`
- Body : `{{ $json.html }}`

- [ ] **Step 6: Tester l'envoi email**

Exécuter le workflow complet manuellement. Vérifier que l'email arrive avec les cartes formatées.

- [ ] **Step 7: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter → workflows/job-agent.json
git add workflows/job-agent.json scripts/email-template.js
git commit -m "feat: HTML email digest generation + SMTP send"
```

---

## Task 8 : Mise à jour Static Data + gestion d'erreurs

**Files:**
- Modify: `workflows/job-agent.json`

### Objectif
Après envoi, marquer les offres comme envoyées. Centraliser le suivi des sources en erreur.

- [ ] **Step 1: Ajouter le nœud "Mark As Sent"**

Ajouter un nœud **Function** nommé `Mark As Sent` après `Send Digest` :

```js
const staticData = $getWorkflowStaticData('global');
if (!staticData.sentUrls) staticData.sentUrls = [];

const now = Date.now();
const offers = $('Build Email HTML').first().json.offers || [];

offers.forEach(offer => {
  if (offer.url) {
    staticData.sentUrls.push({ url: offer.url, ts: now });
  }
});

return [{ json: { markedCount: offers.length } }];
```

- [ ] **Step 2: Ajouter le suivi des erreurs sources**

Pour chaque nœud collecteur, activer le **Error Handling** dans n8n :
- Dans les settings de chaque nœud RSS/HTTP : `On Error → Continue` (ne pas stopper le workflow)

Ajouter un nœud **Function** nommé `Track Errors` en parallèle des collecteurs, relié aux sorties d'erreur :

```js
// Ce nœud collecte les noms des sources qui ont échoué
// Il est connecté via les sorties "error" de chaque collecteur
const failedSource = $input.first().json.node || 'Source inconnue';
const staticData = $getWorkflowStaticData('global');
if (!staticData.failedSources) staticData.failedSources = [];
staticData.failedSources.push(failedSource);
return [{ json: { failedSources: staticData.failedSources } }];
```

> **Note :** Dans n8n, la gestion des erreurs par source est simplifiée : activer `Continue on Fail` sur chaque nœud RSS/HTTP. Les sources en erreur retournent 0 offres, et une note en bas de l'email liste les sources vides (à implémenter via un nœud qui compare les sources attendues vs reçues).

- [ ] **Step 3: Vérification finale du workflow complet**

Exécuter le workflow entier depuis le nœud `Config` (sans le cron). Vérifier :
- [ ] Toutes les sources collectent des offres (ou échouent gracieusement)
- [ ] Les doublons sont supprimés
- [ ] Le filtrage élimine les offres hors critères
- [ ] Le LLM retourne des scores valides
- [ ] L'email reçu contient des cartes HTML correctement formatées
- [ ] Les URLs sont marquées dans Static Data après envoi

- [ ] **Step 4: Activer le Cron**

Activer le nœud **Schedule Trigger** (toggle en haut du nœud). Le workflow se déclenchera automatiquement chaque lundi à 08h00.

- [ ] **Step 5: Exporter et committer**

```bash
cd /Users/louisburette/Desktop/job-agent
# Exporter → workflows/job-agent.json
git add workflows/job-agent.json
git commit -m "feat: mark sent URLs in static data + error handling + activate cron"
```

---

## Récapitulatif des credentials nécessaires

| Service | Type | Où créer |
|---|---|---|
| Claude API | Anthropic API Key | console.anthropic.com |
| InfoJobs | API Key | developer.infojobs.net |
| SMTP | Identifiants serveur mail | Ton provider email |

---

## Hors scope

- Interface de configuration web
- Historique consultable des offres passées
- Multi-utilisateurs
- Scraping Apify (à ajouter si RSS insuffisants sur WTTJ / Indeed)
